# Pilot readiness — adaptive scope

Reviewed 2026-09-14–15 for the 2026-09-15 workday. The
[day plan](../../DAY_PLAN_2026-09-15.md) owns task detail and acceptance criteria;
existing sprint/CAD ledgers own actual execution status. This scope does not
promote deferred tasks or duplicate those ledgers.

## Must-have for this iteration

- A1: preserve every existing input/report on a refused generated rerun. Without
  this, the rehearsal's evidence-preservation contract is false.
- A2: reconcile the current task identifiers and separate the existing historic
  provenance and prospective pilot decision routes. Without this, a sent request
  or an unavailable old export can be mistaken for the wrong gate's disposition.
- Record exact reproduction/check evidence and remaining owner decisions with
  the implementation handoff. Core delivery does not require a same-day reply.

## Nice-to-have after the core work

1. A3: the 24-case matched-control scenario screen. About two focused hours;
   connects current analytical sensitivity to the next measurement priorities.
2. The preserved `c3c34d2` cleanup, through its own checked PR if still useful.
   Lower value than A3; acceptance is behavior/caller preservation, not LOC saved.

## Maybe-later, triggered by evidence

| Item | Observable trigger to consider promotion | Why wait |
| --- | --- | --- |
| Historical metric rerun, EN-S09B | EN-S02's intended schedule, clock/reset semantics and permission-cleared exports have been accepted | Without the denominator and sources, a new percentage is unsupported |
| Physical pilot, EN-R03 | Actual calibration/equipment/site/operator records and prospective protocol approval are accepted; current dependency is explicitly reconciled | Synthetic declarations do not establish readiness |
| CAD work orders | Owner records a specific approved geometry/fixture need, supplies actual inventory and allocates CAD effort | Existing apparatus may answer the first question; invented dimensions cannot |
| FEA implementation or stub demotion | A promoted solver task names a question the lumped model cannot resolve and has accepted geometry, boundary inputs and an implementation budget | Changing a stub today supplies no additional thermal evidence |
| Corrected rendered publication package | Required source/human review is recorded and a specific corrected release is authorized | Re-rendering legacy reports does not validate their field claims |

## Out

- Generic housekeeping, a second solver, new frameworks and toolchain installation
  as ways to occupy owner waiting time; no demonstrated need in this iteration.
- New CAD, fabricated measurements, historic-data replacement, or declaring a
  model validated from intake success; evidence and scope do not support them.
- New paper searches or a third owner questionnaire; current sources and forms
  already support this bounded plan.
- A software deadline for owner/PI replies or physical weather coverage; elapsed
  time cannot supply those inputs.

## Milestone watch

At core delivery and each owner response, check: (1) A1's generation-mode refusal
regression and full checks; (2) accepted historic provenance in EN-S02;
(3) prospective pilot decision plus explicit ledger dependency disposition;
(4) an approved physical need and actual geometry before CAD promotion.
When a trigger fires, review current evidence and deliberately update the existing
ledger. It is a reason to consider the work, not automatic execution authority.
