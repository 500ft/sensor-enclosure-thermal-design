# Day plan — 2026-09-15

**Status: PROPOSAL for owner review.** This document is the reviewable plan, not
a record of completed work. The owner reviews and edits it; the agent builds
only what survives that edit. Nothing here is merged, sent, spent, or claimed
until the corresponding task is actually done and evidenced. Task status lives
in the existing ledgers (`docs/SPRINT_TASKS.csv`, `docs/CAD_TASKS.csv`), not in
this file.

Base: `main` at `2e8da8a` (rehearsal repair chain #13–#15 merged; 78 tests green).
Branch for this plan: `plan/day-2026-09-15`.

## Honest state going in

- The bounded **software/agent lane is complete.** Every `EN-S*` / `EN-D*` /
  `EN-R*` task owned by the agent is `done`; `main` is green at 78 tests.
- The only agent work produced since is a **−15-line dead-code cleanup**,
  already committed on `refactor/ponytail-simplification-20260915`
  (`c3c34d2`), not yet PR'd.
- Everything else remaining on the ledgers is **Owner/External-gated**: it needs
  real deployment provenance, a permission-cleared pilot CSV, PI approval, or an
  authorization to spend CAD hours — none of which the agent can supply.

So the blunt framing for today: agent-buildable-without-owner-input is small
(T1, and T2/T5 once you pick a direction). The high-value forward work (T3, T4)
needs an owner action first. Pick what you want; the agent builds it.

## Candidate tasks

Owner column: **Agent** = agent builds it; **Owner** = only you (or the PI) can
do it; **Owner→Agent** = you decide/authorize, then the agent builds.

| # | Owner | Task | Deliverable | Acceptance | Gate / blocker |
|---|-------|------|-------------|------------|----------------|
| T1 | Agent | Land the dead-code cleanup | PR from `refactor/ponytail-simplification-20260915` → `main` | −15 LOC removed (dead `_HAVE_LEDGER` flag + orphaned import; unreachable `font_size` branch); 78 tests green; FEA `--help` and PI report build byte-unchanged | None — ready now |
| T2 | Owner→Agent | Decide FEA stub disposition | Either keep as-is, **or** demote `thermal_fea_pipeline.py` to docstring + `INSTALL_PENDING_MSG` and move the stage/interface contract into `docs/cad_fea_plan.md` (~−90 LOC) | If demoted: no behavior/claim change, `--check` still reports toolchain status, plan doc carries the interface | You pick keep vs. demote |
| T3 | Owner | Send the deployment-provenance request (`EN-S02`) | `docs/DEPLOYMENT_PROVENANCE_REQUEST.md` actually sent to the PI/data owner | Confirmed intended window, cadence, clock, and authorized exports recorded | You + PI; prepared, not sent |
| T4 | Owner→Agent | Open the CAD lane (`EN-CAD-02` → `EN-CAD-01`) | `cad/enclosure/baseline-inputs.md` filled from **real** enclosure dimensions (Owner), then `cad/enclosure/parameters.csv` + inspection-to-interface map (Agent) | Parameters trace to `docs/cad_fea_plan.md` §3.1; a variant matrix shows which variables change; no invented lab dimensions | You supply real geometry + authorize CAD hours (currently `deferred`) |
| T5 | Agent | Housekeeping (optional, low value) | `/ponytail-debt` ledger of any deferred-shortcut comments; confirm generated artifacts under `output/` stay gitignored | Report only; no source change unless a real leak is found | None — include only if capacity |

## Recommended order for today

1. **T1** — merge the cleanup; it is done and green, no reason to leave it in limbo.
2. **T2** — one decision from you; agent executes in minutes either way.
3. **T3** — the single highest-leverage move on the whole board, because it is
   the gate on `EN-S09B` (real field metrics) and feeds `EN-R03`. It is a *you*
   action, not an agent one.
4. **T4** — only if you want to start CAD and can provide real geometry today;
   otherwise it stays parked, honestly.

## Boundary and next gate

No spending, outreach, publication, deployment, powered-hardware operation, or
fabricated approval is authorized by this plan. The agent cannot supply real
site/equipment identity, PI/data permission, or physical measurements — those
remain external gates. If an input is missing, the agent records the exact
attempted source and the access limit rather than substituting an unsupported
fact. `EN-R03` and `EN-S11` (physical-agreement claim; independent human review)
stay blocked regardless of what is built today.
